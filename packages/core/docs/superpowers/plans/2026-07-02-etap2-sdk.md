# Etap 2 (SDK) — Plan Implementacji dla `tracelyx-sdk`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Domknięcie tasków Etapu 2 roadmapy (`../tasks-roadmap.md`) w zakresie SDK: TASK-211 (LangGraph), TASK-212 (OpenAI Agents), TASK-230 (`tracelyx validate`) oraz SDK-owa część TASK-221 (klasyfikacja `errorType`).

**Architecture:** Wszystkie zmiany w `packages/core`. Integracje monkey-patchują obiekty third-party i wysyłają `SpanPayload` przez `tracelyxClient.recordSpan()`; kontekst parent-child propagowany przez `AsyncLocalStorage` (`getActiveContext`/`runWithContext` z `src/tracer.ts`). CLI (`bin/tracelyx.ts`) to router komend z eksportowanymi funkcjami dla testowalności.

**Tech Stack:** TypeScript, tsup (dual ESM+CJS build), vitest, natywny `fetch`. Zero runtime dependencies.

## Global Constraints

- Zero runtime dependencies w `packages/core` (AD-03 z roadmapy) — żadnych nowych pakietów w `dependencies`.
- Rozmiar bundla `dist/index.js` < 20KB gzip — weryfikować po każdej rozbudowie (AD-03).
- Dual build ESM + CJS przez tsup; bin entry bez DTS (patrz `tsup.config.ts`).
- Konwencja commitów: conventional commits (`feat(core):`, `test(langgraph):`, `fix(bin):` — jak w historii git).
- Wszystkie testy: `vitest`, mock `fetch` przez `vi.stubGlobal('fetch', ...)`, mock client przez `new TracelyxClient({ apiKey: 'tl_test', projectId: 'proj_1' })` + `client.flush()` + parsowanie `fetchMock.mock.calls[0][1].body` — dokładnie jak w istniejących testach `__tests__/integrations/`.
- Po każdym tasku: `pnpm exec vitest run` (całość) musi przechodzić, na koniec planu `pnpm check-types` i `pnpm build` z repo root.
- Wszystkie komendy testowe uruchamiać z katalogu `packages/core`.

## Stan zastany (czego NIE robimy, bo już jest)

| Kryterium z roadmapy | Stan |
|---|---|
| TASK-211: `instrumentLangGraph(graph, client)`, span `agent_step` per invoke, per-node spany w `stream()`, `thread_id`/`checkpoint_id` na invoke spanie, idempotencja, warning gdy brak `streamEvents` | ✅ zrobione (`src/integrations/langgraph.ts`, 9 testów) |
| TASK-212: `instrumentOpenAIAgents(agent, client)`, span per `run()`, tool_call child spany z `tool.name`/`tool.arguments`, `handoff.target_agent` na agent spanie, `openai.model`, propagacja traceId do zagnieżdżonych runów | ✅ zrobione (`src/integrations/openai-agents.ts`, 10 testów) |
| TASK-230: komenda `validate` z `--api-key`, `--project-id`, `--tenant`, `--json`, exit codes, komunikaty błędów, timeout 10s | ✅ zrobione (`bin/tracelyx.ts`, 12 testów) |

## Luki do domknięcia (zakres tego planu)

1. **TASK-221 (SDK):** brak klasyfikacji `errorType` — SDK ma automatycznie wypełniać typ błędu (`tool_timeout | context_window_exceeded | json_parse_error | rate_limit | network_error | hook_error | unknown`) na podstawie wyjątku.
2. **TASK-211:** node spany ze `stream()` nie mają `langgraph.node_name` (roadmapa wymaga tej nazwy atrybutu) ani `langgraph.thread_id`; brak patcha `streamEvents` (dokładne czasy per węzeł); brak testu subgrafów.
3. **TASK-212:** handoff target agents (`agent.handoffs`) nie są auto-instrumentowane — trace NIE propaguje się przez handoff, jeśli developer nie zinstrumentuje ręcznie każdego agenta; brak wsparcia dla `Runner` (roadmapa: `instrumentOpenAIAgents(agentOrRunner, ...)`); tool span `transfer_to_*` nie ma atrybutu `handoff.target_agent`.
4. **TASK-230:** brak potwierdzenia receipt przez GET na trace (AC pkt 3) i weryfikacji routingu `--tenant` (AC pkt 5).

**Poza zakresem:** TASK-240 (GitHub Action) — mimo obszaru `sdk + infra` żyje w osobnym repo lub `apps/github-action/` głównego monorepo i zależy od endpointów eval API (TASK-053), które są w repo backendu. TASK-210/220/221(backend+frontend)/222/231 — obszary backend/frontend, poza tym repozytorium.

**Kontrakt z backendem (założenie):** Task 8 zakłada, że Ingestion API wystawia `GET /v1/traces/:traceId` (Bearer auth) zwracające `{ id, tenantId?, ... }` z HTTP 200 gdy trace dotarł, 404 gdy nie. Jeśli backend tego jeszcze nie ma — zaimplementować po stronie `apps/ingestion-api` w głównym repo; komunikat błędu w CLI jasno wskazuje tę sytuację.

---

### Task 1: Moduł klasyfikacji błędów `classifyError` (TASK-221)

**Files:**
- Create: `packages/core/src/errors.ts`
- Test: `packages/core/__tests__/errors.test.ts`

**Interfaces:**
- Consumes: nic (czysty moduł).
- Produces: `export type ErrorType = 'tool_timeout' | 'context_window_exceeded' | 'json_parse_error' | 'rate_limit' | 'network_error' | 'hook_error' | 'unknown'` oraz `export function classifyError(error: unknown): ErrorType`. Task 2 i 9 z tego korzystają.

- [ ] **Step 1: Write the failing test**

Utwórz `packages/core/__tests__/errors.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { classifyError } from '../src/errors.js';

describe('classifyError', () => {
  it('classifies SyntaxError as json_parse_error', () => {
    expect(classifyError(new SyntaxError('Unexpected token'))).toBe('json_parse_error');
  });

  it('classifies HTTP 429 status as rate_limit', () => {
    const err = Object.assign(new Error('Request failed'), { status: 429 });
    expect(classifyError(err)).toBe('rate_limit');
  });

  it('classifies "rate limit" message as rate_limit', () => {
    expect(classifyError(new Error('Anthropic rate limit exceeded'))).toBe('rate_limit');
  });

  it('classifies context window messages as context_window_exceeded', () => {
    expect(classifyError(new Error('prompt is too long: 250000 tokens'))).toBe('context_window_exceeded');
    expect(classifyError(new Error('maximum context length is 128000 tokens'))).toBe('context_window_exceeded');
    expect(classifyError(new Error('context window exceeded'))).toBe('context_window_exceeded');
  });

  it('classifies AbortError / timeout messages as tool_timeout', () => {
    const abortErr = Object.assign(new Error('The operation was aborted'), { name: 'AbortError' });
    expect(classifyError(abortErr)).toBe('tool_timeout');
    expect(classifyError(new Error('Request timed out after 30000ms'))).toBe('tool_timeout');
  });

  it('classifies network error codes and fetch failures as network_error', () => {
    const connRefused = Object.assign(new Error('connect ECONNREFUSED'), { code: 'ECONNREFUSED' });
    expect(classifyError(connRefused)).toBe('network_error');
    expect(classifyError(new TypeError('fetch failed'))).toBe('network_error');
  });

  it('rate_limit wins over timeout when both signals present (429 + "timeout" in message)', () => {
    const err = Object.assign(new Error('timeout waiting for rate limiter'), { status: 429 });
    expect(classifyError(err)).toBe('rate_limit');
  });

  it('returns unknown for plain errors and non-Error values', () => {
    expect(classifyError(new Error('something odd'))).toBe('unknown');
    expect(classifyError('a string')).toBe('unknown');
    expect(classifyError(null)).toBe('unknown');
    expect(classifyError(undefined)).toBe('unknown');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec vitest run __tests__/errors.test.ts`
Expected: FAIL — `Cannot find module '../src/errors.js'`

- [ ] **Step 3: Write minimal implementation**

Utwórz `packages/core/src/errors.ts`:

```typescript
/** Matches ErrorType enum in @tracelyx/shared (TASK-221). */
export type ErrorType =
  | 'tool_timeout'
  | 'context_window_exceeded'
  | 'json_parse_error'
  | 'rate_limit'
  | 'network_error'
  | 'hook_error'
  | 'unknown';

const NETWORK_CODES = new Set(['ECONNREFUSED', 'ECONNRESET', 'ENOTFOUND', 'ETIMEDOUT', 'EAI_AGAIN']);

export function classifyError(error: unknown): ErrorType {
  if (error instanceof SyntaxError) return 'json_parse_error';
  if (error === null || typeof error !== 'object') return 'unknown';

  const err = error as { name?: string; message?: string; status?: number; code?: string };
  const message = (err.message ?? '').toLowerCase();

  if (err.status === 429 || message.includes('rate limit') || message.includes('too many requests')) {
    return 'rate_limit';
  }
  if (
    message.includes('context window') ||
    message.includes('context length') ||
    message.includes('prompt is too long')
  ) {
    return 'context_window_exceeded';
  }
  if (err.name === 'AbortError' || err.name === 'TimeoutError' || message.includes('timed out') || message.includes('timeout')) {
    return 'tool_timeout';
  }
  if ((err.code !== undefined && NETWORK_CODES.has(err.code)) || message.includes('fetch failed') || message.includes('network')) {
    return 'network_error';
  }
  return 'unknown';
}
```

Uwaga na kolejność gałęzi — jest częścią kontraktu (test „rate_limit wins over timeout"): `json_parse_error` → `rate_limit` → `context_window_exceeded` → `tool_timeout` → `network_error` → `unknown`. `hook_error` nie jest wykrywany heurystyką — przypisuje go wprost CLI hook (Task 2, Step 5).

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm exec vitest run __tests__/errors.test.ts`
Expected: PASS (8 tests)

- [ ] **Step 5: Export z publicznego API**

W `packages/core/src/index.ts` dodaj linię (obok istniejących eksportów):

```typescript
export { classifyError } from './errors.js';
export type { ErrorType } from './errors.js';
```

Run: `pnpm check-types`
Expected: brak błędów

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/errors.ts packages/core/__tests__/errors.test.ts packages/core/src/index.ts
git commit -m "feat(core): add classifyError utility for errorType classification"
```

---

### Task 2: Wpięcie `error.type` w Span, integracje i CLI hook (TASK-221)

**Files:**
- Modify: `packages/core/src/tracer.ts` (metoda `Span.recordError`, ~linia 56)
- Modify: `packages/core/src/integrations/langgraph.ts` (catch w `invoke`, ~linia 100)
- Modify: `packages/core/src/integrations/openai-agents.ts` (catch w `wrapTools` ~linia 43 i w `run` ~linia 107)
- Modify: `packages/core/src/integrations/anthropic.ts` (blok catch — znajdź `recordError` lub `error.message` w pliku i dodaj analogicznie)
- Modify: `packages/core/bin/tracelyx.ts` (`buildHookSpan`, ~linia 47)
- Test: `packages/core/__tests__/tracer.test.ts`, `packages/core/__tests__/integrations/langgraph.test.ts`, `packages/core/__tests__/integrations/openai-agents.test.ts`, `packages/core/__tests__/bin/hook.test.ts`

**Interfaces:**
- Consumes: `classifyError(error: unknown): ErrorType` z Task 1.
- Produces: każdy span ze statusem `error` niesie `attributes['error.type']: ErrorType`. Backend (TASK-221 w repo głównym) mapuje ten atrybut na `TraceDocument.errorType`.

- [ ] **Step 1: Write the failing tests**

Dopisz do `packages/core/__tests__/tracer.test.ts` (na końcu głównego `describe`, używając wzorca tworzenia trace obecnego w tym pliku — jeśli plik tworzy `Trace` przez `client.startTrace(...)`, zrób tak samo):

```typescript
it('recordError sets error.type attribute from classifyError', async () => {
  const spans: SpanPayload[] = [];
  const trace = new Trace((p) => spans.push(p));
  const span = trace.startSpan('failing-step', 'tool_call');

  span.recordError(Object.assign(new Error('Request failed'), { status: 429 }));
  span.end();

  expect(spans[0].status).toBe('error');
  expect(spans[0].attributes['error.type']).toBe('rate_limit');
});

it('trace.trace() sets error.type when wrapped fn throws', async () => {
  const spans: SpanPayload[] = [];
  const trace = new Trace((p) => spans.push(p));

  await expect(
    trace.trace('step', async () => {
      throw new Error('Request timed out after 5000ms');
    }),
  ).rejects.toThrow('timed out');

  expect(spans[0].attributes['error.type']).toBe('tool_timeout');
});
```

(Jeśli `tracer.test.ts` nie importuje `Trace`/`SpanPayload` bezpośrednio — dodaj importy: `import { Trace } from '../src/tracer.js';` i `import type { SpanPayload } from '../src/types.js';`.)

Dopisz do `__tests__/integrations/langgraph.test.ts` (w głównym `describe`, korzysta z `fetchMock`/`client` z `beforeEach`):

```typescript
it('sets error.type attribute when invoke throws', async () => {
  const graph = { invoke: vi.fn().mockRejectedValue(new Error('rate limit exceeded')) };
  instrumentLangGraph(graph, client);

  await expect(graph.invoke({})).rejects.toThrow('rate limit');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  expect(body.spans[0].attributes['error.type']).toBe('rate_limit');
});
```

Dopisz do `__tests__/integrations/openai-agents.test.ts` analogiczny test (mock agenta wg wzorca z tego pliku — agent z `run: vi.fn().mockRejectedValue(...)`):

```typescript
it('sets error.type attribute when run() throws', async () => {
  const agent = { name: 'helper', run: vi.fn().mockRejectedValue(new Error('fetch failed')) };
  instrumentOpenAIAgents(agent, client);

  await expect(agent.run('input')).rejects.toThrow('fetch failed');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const agentSpan = body.spans.find((s) => s.kind === 'agent_step')!;
  expect(agentSpan.attributes['error.type']).toBe('network_error');
});
```

Dopisz do `__tests__/bin/hook.test.ts` (wg wzorca istniejących testów hook — mock fetch + `runHookCommand` z env `TRACELYX_API_KEY`/`TRACELYX_PROJECT_ID`):

```typescript
it('sets error.type=hook_error and status=error when tool_error present', async () => {
  vi.stubEnv('TRACELYX_API_KEY', 'tl_test');
  vi.stubEnv('TRACELYX_PROJECT_ID', 'proj_1');
  const fetchMock = vi.fn().mockResolvedValue(new Response('{"accepted":1}', { status: 200 }));
  vi.stubGlobal('fetch', fetchMock);

  await runHookCommand(
    ['--event', 'PostToolUse'],
    JSON.stringify({ session_id: 's-1', tool_name: 'Bash', tool_error: 'command not found' }),
  );

  const body = JSON.parse(fetchMock.mock.calls[0][1].body);
  expect(body.spans[0].status).toBe('error');
  expect(body.spans[0].attributes['error.type']).toBe('hook_error');
  expect(body.spans[0].attributes['hook.error']).toBe('command not found');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm exec vitest run`
Expected: FAIL — nowe testy czerwone (brak `error.type`), stare zielone

- [ ] **Step 3: Implementacja**

W `src/tracer.ts` — import na górze: `import { classifyError } from './errors.js';`, a w `Span.recordError`:

```typescript
  recordError(error: Error): void {
    this.status = 'error';
    this.attrs['error.message'] = error.message;
    this.attrs['error.stack'] = error.stack ?? '';
    this.attrs['error.type'] = classifyError(error);
  }
```

W `src/integrations/langgraph.ts` — import: `import { classifyError } from '../errors.js';`, w bloku `catch` metody `invoke`:

```typescript
    } catch (error) {
      status = 'error';
      attributes['error.type'] = classifyError(error);
      if (error instanceof Error) {
        attributes['error.message'] = error.message;
        attributes['error.stack'] = error.stack;
      }
      throw error;
    }
```

W `src/integrations/openai-agents.ts` — ten sam import; identyczna wstawka `attributes['error.type'] = classifyError(error);` w OBU blokach catch (w `wrapTools` i w `agentAsAny.run`).

W `src/integrations/anthropic.ts` — ten sam import; jeśli plik używa `Span.recordError` — nic nie trzeba robić (Step 1 w tracer.ts to załatwia); jeśli buduje `attributes` ręcznie w catch — dodaj tę samą linię.

W `bin/tracelyx.ts` — import: `import { classifyError } from '../src/errors.js';` (import wystarczy dla spójności typu; sama klasyfikacja hooka jest stała). W `buildHookSpan` zmień pole `status` i dodaj atrybut:

```typescript
  const hasError = typeof hookData['tool_error'] === 'string';

  return {
    id: randomUUID(),
    traceId: sessionId,
    parentSpanId: null,
    name: `hook.${hookName}`,
    kind: 'hook',
    startTime: now - executionMs,
    endTime: now,
    durationMs: executionMs,
    status: hasError ? 'error' : 'ok',
    attributes: {
      'hook.name': hookName,
      'hook.session_id': sessionId,
      ...(hasError && { 'error.type': 'hook_error' }),
      // ... reszta istniejących spreadów bez zmian
```

(Jeśli import `classifyError` w `bin/tracelyx.ts` okaże się nieużywany — nie dodawaj go; lint/DCE ma być czysty.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm exec vitest run`
Expected: PASS — wszystkie, w tym stare testy hook (sprawdź, czy któryś stary test nie asertował `status: 'ok'` przy `tool_error` — jeśli tak, zaktualizuj go, to zamierzona zmiana zachowania)

- [ ] **Step 5: Commit**

```bash
git add packages/core/src packages/core/bin packages/core/__tests__
git commit -m "feat(core): auto-classify errorType on error spans (TASK-221 SDK part)"
```

---

### Task 3: LangGraph — `langgraph.node_name` i `thread_id` na node spanach (TASK-211)

**Files:**
- Modify: `packages/core/src/integrations/langgraph.ts` (patch `stream`, ~linie 57–73)
- Test: `packages/core/__tests__/integrations/langgraph.test.ts`

**Interfaces:**
- Consumes: istniejący patch `stream()`.
- Produces: node spany z atrybutami `langgraph.node_name` (wymóg roadmapy), `langgraph.node` (backward compat — dashboard może już go czytać) i `langgraph.thread_id` (z `config.configurable.thread_id`).

- [ ] **Step 1: Write the failing test**

Dopisz do `__tests__/integrations/langgraph.test.ts`:

```typescript
it('node spans carry langgraph.node_name and langgraph.thread_id from config', async () => {
  async function* fakeStream() {
    yield { researcher: { results: ['a'] } };
  }
  const graph = {
    invoke: vi.fn().mockResolvedValue({}),
    stream: vi.fn().mockReturnValue(fakeStream()),
  };
  instrumentLangGraph(graph, client);

  for await (const _chunk of (graph as any).stream({}, { configurable: { thread_id: 'thread-42' } })) {
    // drain
  }
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const nodeSpan = body.spans.find((s) => s.name === 'langgraph.node.researcher')!;
  expect(nodeSpan.attributes['langgraph.node_name']).toBe('researcher');
  expect(nodeSpan.attributes['langgraph.node']).toBe('researcher');
  expect(nodeSpan.attributes['langgraph.thread_id']).toBe('thread-42');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec vitest run __tests__/integrations/langgraph.test.ts`
Expected: FAIL — `langgraph.node_name` undefined

- [ ] **Step 3: Implementacja**

W patchu `stream` w `src/integrations/langgraph.ts` zmień budowę atrybutów node spana:

```typescript
            const nodeSpan: SpanPayload = {
              id: randomUUID(),
              traceId: streamTraceId,
              parentSpanId: streamParentSpanId,
              name: `langgraph.node.${nodeName}`,
              kind: 'agent_step',
              startTime: prevTime,
              endTime: now,
              durationMs: now - prevTime,
              status: 'ok',
              attributes: {
                'langgraph.node': nodeName,
                'langgraph.node_name': nodeName,
                ...(config?.configurable?.thread_id !== undefined && {
                  'langgraph.thread_id': config.configurable.thread_id,
                }),
              },
              tenantId: getActiveContext()?.tenantId,
            };
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm exec vitest run __tests__/integrations/langgraph.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/integrations/langgraph.ts packages/core/__tests__/integrations/langgraph.test.ts
git commit -m "feat(langgraph): add node_name and thread_id attributes to node spans"
```

---

### Task 4: LangGraph — patch `streamEvents` z dokładnymi czasami per węzeł (TASK-211)

**Files:**
- Modify: `packages/core/src/integrations/langgraph.ts`
- Test: `packages/core/__tests__/integrations/langgraph.test.ts`

**Interfaces:**
- Consumes: `getActiveContext`, `runWithContext`, `tracelyxClient.recordSpan`.
- Produces: patch `graph.streamEvents(input, options)` — dla par eventów `on_chain_start`/`on_chain_end` z `metadata.langgraph_node` emituje span `agent_step` z rzeczywistym `startTime`/`endTime` (a nie przybliżeniem „od poprzedniego chunka" jak w `stream()`).

Kontekst: LangGraph JS >= 0.2.0 wystawia `streamEvents` (LangChain Runnable API, `version: 'v2'`). Eventy wykonania węzła mają kształt `{ event: 'on_chain_start' | 'on_chain_end', name: string, run_id: string, metadata: { langgraph_node?: string } }`. Korelujemy start/end po `run_id`.

- [ ] **Step 1: Write the failing test**

Dopisz do `__tests__/integrations/langgraph.test.ts`:

```typescript
it('streamEvents patch emits node spans with accurate start/end pairing by run_id', async () => {
  async function* fakeEvents() {
    yield { event: 'on_chain_start', name: 'LangGraph', run_id: 'root', metadata: {} };
    yield { event: 'on_chain_start', name: 'researcher', run_id: 'r1', metadata: { langgraph_node: 'researcher' } };
    yield { event: 'on_chain_end', name: 'researcher', run_id: 'r1', metadata: { langgraph_node: 'researcher' } };
    yield { event: 'on_chain_start', name: 'writer', run_id: 'w1', metadata: { langgraph_node: 'writer' } };
    yield { event: 'on_chain_end', name: 'writer', run_id: 'w1', metadata: { langgraph_node: 'writer' } };
    yield { event: 'on_chain_end', name: 'LangGraph', run_id: 'root', metadata: {} };
  }
  const graph = {
    invoke: vi.fn().mockResolvedValue({}),
    stream: vi.fn(),
    streamEvents: vi.fn().mockReturnValue(fakeEvents()),
  };
  instrumentLangGraph(graph, client);

  const seen: unknown[] = [];
  for await (const ev of (graph as any).streamEvents({}, { version: 'v2' })) {
    seen.push(ev);
  }
  await client.flush();

  expect(seen).toHaveLength(6); // eventy przechodzą nietknięte

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const names = body.spans.map((s) => s.name).sort();
  expect(names).toEqual(['langgraph.node.researcher', 'langgraph.node.writer']);
  for (const span of body.spans) {
    expect(span.kind).toBe('agent_step');
    expect(span.attributes['langgraph.node_name']).toBeDefined();
    expect(span.endTime).toBeGreaterThanOrEqual(span.startTime);
  }
});

it('streamEvents ignores unmatched on_chain_end and non-node events', async () => {
  async function* fakeEvents() {
    yield { event: 'on_chat_model_stream', name: 'ChatAnthropic', run_id: 'x1', metadata: {} };
    yield { event: 'on_chain_end', name: 'ghost', run_id: 'never-started', metadata: { langgraph_node: 'ghost' } };
  }
  const graph = {
    invoke: vi.fn().mockResolvedValue({}),
    streamEvents: vi.fn().mockReturnValue(fakeEvents()),
  };
  instrumentLangGraph(graph, client);

  for await (const _ev of (graph as any).streamEvents({})) {
    // drain
  }
  await client.flush();

  expect(fetchMock).not.toHaveBeenCalled(); // zero spanów
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec vitest run __tests__/integrations/langgraph.test.ts`
Expected: FAIL — spany nie są emitowane (patch nie istnieje)

- [ ] **Step 3: Implementacja**

W `src/integrations/langgraph.ts`, po bloku patchującym `stream` (a przed patchem `invoke`), dodaj:

```typescript
interface StreamEventLike {
  event?: string;
  name?: string;
  run_id?: string;
  metadata?: { langgraph_node?: string; [key: string]: unknown };
}
```

(interfejs na poziomie modułu, obok `LangGraphConfig`), a w `instrumentLangGraph`:

```typescript
  if (typeof graphAsAny.streamEvents === 'function') {
    const originalStreamEvents = graphAsAny.streamEvents.bind(graphAsAny);

    graphAsAny.streamEvents = async function* (
      input: unknown,
      options?: LangGraphConfig,
      ...rest: unknown[]
    ): AsyncGenerator<unknown> {
      const ctx = getActiveContext();
      const traceId = ctx?.traceId ?? randomUUID();
      const parentSpanId = ctx?.spanId ?? null;
      const openNodes = new Map<string, { name: string; startTime: number }>();

      for await (const event of originalStreamEvents(input, options, ...rest)) {
        const e = event as StreamEventLike;
        const nodeName = e.metadata?.langgraph_node;

        if (nodeName !== undefined && e.run_id !== undefined) {
          if (e.event === 'on_chain_start' && e.name === nodeName) {
            openNodes.set(e.run_id, { name: nodeName, startTime: Date.now() });
          } else if (e.event === 'on_chain_end' && openNodes.has(e.run_id)) {
            const { name, startTime } = openNodes.get(e.run_id)!;
            openNodes.delete(e.run_id);
            const now = Date.now();
            tracelyxClient.recordSpan({
              id: randomUUID(),
              traceId,
              parentSpanId,
              name: `langgraph.node.${name}`,
              kind: 'agent_step',
              startTime,
              endTime: now,
              durationMs: now - startTime,
              status: 'ok',
              attributes: {
                'langgraph.node': name,
                'langgraph.node_name': name,
                ...(options?.configurable?.thread_id !== undefined && {
                  'langgraph.thread_id': options.configurable.thread_id,
                }),
              },
              tenantId: ctx?.tenantId,
            });
          }
        }

        yield event;
      }
    };
  }
```

Uwaga: warunek `e.name === nodeName` przy `on_chain_start` odsiewa wewnętrzne runnable'e działające „w środku" węzła (one też dziedziczą `metadata.langgraph_node`, ale mają inny `name`).

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm exec vitest run __tests__/integrations/langgraph.test.ts`
Expected: PASS (wszystkie, łącznie ze starymi — w tym `emits console.warn when streamEvents is absent`)

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/integrations/langgraph.ts packages/core/__tests__/integrations/langgraph.test.ts
git commit -m "feat(langgraph): patch streamEvents for accurate per-node spans"
```

---

### Task 5: LangGraph — subgrafy jako child spany (TASK-211, test weryfikacyjny)

**Files:**
- Test: `packages/core/__tests__/integrations/langgraph.test.ts`

**Interfaces:**
- Consumes: istniejący patch `invoke` (używa `runWithContext`, więc zagnieżdżony `invoke` subgrafu powinien dziedziczyć kontekst rodzica przez `AsyncLocalStorage`).
- Produces: potwierdzenie AC „Subgrafy: span subgraph jest child span parent graph node". To task czysto testowy — jeśli test przejdzie od razu, kod jest gotowy; jeśli nie, debuguj propagację kontekstu w patchu `invoke`.

- [ ] **Step 1: Write the test**

Dopisz do `__tests__/integrations/langgraph.test.ts`:

```typescript
it('nested instrumented subgraph invoke becomes a child span of parent invoke', async () => {
  const subgraph: any = { invoke: vi.fn().mockResolvedValue({ sub: 'done' }) };
  instrumentLangGraph(subgraph, client);

  const parent: any = {
    invoke: vi.fn().mockImplementation(async () => {
      // węzeł grafu rodzica wywołuje subgraf
      return subgraph.invoke({ nested: true });
    }),
  };
  instrumentLangGraph(parent, client);

  await parent.invoke({ input: 'top' });
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  expect(body.spans).toHaveLength(2);

  const spans = [...body.spans].sort((a, b) => (a.parentSpanId === null ? -1 : 1));
  const [parentSpan, childSpan] = spans;
  expect(parentSpan.parentSpanId).toBeNull();
  expect(childSpan.parentSpanId).toBe(parentSpan.id);
  expect(childSpan.traceId).toBe(parentSpan.traceId);
});
```

- [ ] **Step 2: Run test**

Run: `pnpm exec vitest run __tests__/integrations/langgraph.test.ts`
Expected: PASS od razu (mechanizm `runWithContext` już to obsługuje). Jeśli FAIL: sprawdź, czy `mockImplementation` rodzica jest wywoływany wewnątrz `runWithContext` — patch woła `originalInvoke` w callbacku `runWithContext({ spanId, traceId }, ...)`, więc kontekst musi być widoczny.

- [ ] **Step 3: Commit**

```bash
git add packages/core/__tests__/integrations/langgraph.test.ts
git commit -m "test(langgraph): verify subgraph invoke nests under parent invoke span"
```

---

### Task 6: OpenAI Agents — auto-instrumentacja handoff targets + `handoff.target_agent` na tool spanie (TASK-212)

**Files:**
- Modify: `packages/core/src/integrations/openai-agents.ts`
- Test: `packages/core/__tests__/integrations/openai-agents.test.ts`

**Interfaces:**
- Consumes: istniejące `instrumentOpenAIAgents`, `wrapTools`.
- Produces: (1) agenty z `agent.handoffs` (tablica agentów lub obiektów `{ agent }`) są rekurencyjnie instrumentowane — trace ID propaguje się przez cały multi-agent run bez ręcznej instrumentacji każdego agenta; (2) tool span `transfer_to_*` dostaje `attributes['handoff.target_agent']`.

- [ ] **Step 1: Write the failing tests**

Dopisz do `__tests__/integrations/openai-agents.test.ts`:

```typescript
it('auto-instruments handoff target agents so trace propagates through handoffs', async () => {
  const billingAgent: any = {
    name: 'billing',
    run: vi.fn().mockResolvedValue('billing done'),
  };
  const triageAgent: any = {
    name: 'triage',
    handoffs: [billingAgent],
    run: vi.fn().mockImplementation(async () => billingAgent.run('handed off')),
  };
  instrumentOpenAIAgents(triageAgent, client);

  await triageAgent.run('help me with my invoice');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const triageSpan = body.spans.find((s) => s.name === 'agent.triage')!;
  const billingSpan = body.spans.find((s) => s.name === 'agent.billing')!;
  expect(billingSpan.traceId).toBe(triageSpan.traceId);
  expect(billingSpan.parentSpanId).toBe(triageSpan.id);
});

it('supports handoff objects wrapping the agent ({ agent } shape)', async () => {
  const target: any = { name: 'refunds', run: vi.fn().mockResolvedValue('ok') };
  const source: any = {
    name: 'support',
    handoffs: [{ agent: target, toolName: 'transfer_to_refunds' }],
    run: vi.fn().mockResolvedValue('ok'),
  };
  instrumentOpenAIAgents(source, client);

  await target.run('direct call after handoff');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  expect(body.spans.some((s) => s.name === 'agent.refunds')).toBe(true);
});

it('sets handoff.target_agent attribute on the transfer_to_ tool span', async () => {
  const transferTool: any = {
    name: 'transfer_to_billing',
    on_invoke_tool: vi.fn().mockResolvedValue('transferred'),
  };
  const agent: any = {
    name: 'triage',
    tools: [transferTool],
    run: vi.fn().mockImplementation(async () => transferTool.on_invoke_tool({}, '{}')),
  };
  instrumentOpenAIAgents(agent, client);

  await agent.run('input');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const toolSpan = body.spans.find((s) => s.kind === 'tool_call')!;
  expect(toolSpan.attributes['handoff.target_agent']).toBe('billing');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm exec vitest run __tests__/integrations/openai-agents.test.ts`
Expected: FAIL — brak spana `agent.billing` (handoff target nie instrumentowany), brak `handoff.target_agent` na tool spanie

- [ ] **Step 3: Implementacja**

W `src/integrations/openai-agents.ts`:

(a) Rozszerz `AgentLike` o handoffs:

```typescript
interface AgentLike {
  name?: string;
  model?: string;
  tools?: ToolLike[];
  handoffs?: unknown[];
  run(...args: unknown[]): Promise<unknown>;
  [key: string | symbol]: unknown;
}
```

(b) W `wrapTools`, w bloku `finally` (przed budową `toolSpan`), dodaj atrybut handoff:

```typescript
      } finally {
        const endTime = Date.now();
        if (toolName.startsWith('transfer_to_')) {
          const target = toolName.slice('transfer_to_'.length);
          handoffTargets.add(target);
          attributes['handoff.target_agent'] = target;
        }
        const toolSpan: SpanPayload = {
          // ... bez zmian
```

(c) W `instrumentOpenAIAgents`, po bloku `if (Array.isArray(agentAsAny.tools)) { ... }`, dodaj rekurencyjną instrumentację handoffów:

```typescript
  if (Array.isArray(agentAsAny.handoffs)) {
    for (const handoff of agentAsAny.handoffs) {
      const target =
        handoff !== null && typeof handoff === 'object' && 'agent' in (handoff as object)
          ? (handoff as { agent: unknown }).agent
          : handoff;
      if (
        target !== null &&
        typeof target === 'object' &&
        typeof (target as AgentLike).run === 'function'
      ) {
        instrumentOpenAIAgents(target as AgentLike, tracelyxClient);
      }
    }
  }
```

Rekurencja jest bezpieczna: symbol `INSTRUMENTED` przerywa cykle (A ↔ B w handoffach nie zapętli się).

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm exec vitest run __tests__/integrations/openai-agents.test.ts`
Expected: PASS (14 testów — 10 istniejących + 1 z Task 2 + 3 nowe)

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/integrations/openai-agents.ts packages/core/__tests__/integrations/openai-agents.test.ts
git commit -m "feat(openai-agents): auto-instrument handoff targets, tag transfer tool spans"
```

---

### Task 7: OpenAI Agents — wsparcie `Runner` (TASK-212: `agentOrRunner`)

**Files:**
- Modify: `packages/core/src/integrations/openai-agents.ts`
- Test: `packages/core/__tests__/integrations/openai-agents.test.ts`

**Interfaces:**
- Consumes: `instrumentOpenAIAgents` z Task 6.
- Produces: `instrumentOpenAIAgents(runner, client)` gdzie runner ma sygnaturę `run(agent, input, options?)` (wzorzec `Runner` z `@openai/agents`). Patch tworzy span `agent_step` per wywołanie `runner.run` (nazwany po `agent.name`) i instrumentuje przekazanego agenta (tools + handoffs). Detekcja: obiekt z `run` bez `name` i bez `tools` = runner; z `name` lub `tools` = agent.

- [ ] **Step 1: Write the failing tests**

Dopisz do `__tests__/integrations/openai-agents.test.ts`:

```typescript
it('instruments a Runner: creates agent_step span named after the passed agent', async () => {
  const agent: any = { name: 'assistant', model: 'gpt-4o', tools: [] };
  const runner: any = {
    run: vi.fn().mockResolvedValue({ finalOutput: 'done' }),
  };
  instrumentOpenAIAgents(runner, client);

  const result = await runner.run(agent, 'user input');
  expect(result).toEqual({ finalOutput: 'done' });
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const span = body.spans[0];
  expect(span.name).toBe('agent.assistant');
  expect(span.kind).toBe('agent_step');
  expect(span.attributes['agent.name']).toBe('assistant');
  expect(span.attributes['openai.model']).toBe('gpt-4o');
});

it('Runner patch instruments the passed agent tools (tool spans are children of run span)', async () => {
  const tool: any = { name: 'search', on_invoke_tool: vi.fn().mockResolvedValue('found') };
  const agent: any = { name: 'assistant', tools: [tool] };
  const runner: any = {
    run: vi.fn().mockImplementation(async (a: any) => a.tools[0].on_invoke_tool({}, '{"q":"x"}')),
  };
  instrumentOpenAIAgents(runner, client);

  await runner.run(agent, 'input');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  const runSpan = body.spans.find((s) => s.kind === 'agent_step')!;
  const toolSpan = body.spans.find((s) => s.kind === 'tool_call')!;
  expect(toolSpan.parentSpanId).toBe(runSpan.id);
  expect(toolSpan.traceId).toBe(runSpan.traceId);
});

it('runner instrumentation is idempotent and records errors with error.type', async () => {
  const agent: any = { name: 'assistant' };
  const runner: any = { run: vi.fn().mockRejectedValue(new Error('rate limit exceeded')) };
  instrumentOpenAIAgents(runner, client);
  instrumentOpenAIAgents(runner, client);

  await expect(runner.run(agent, 'x')).rejects.toThrow('rate limit');
  await client.flush();

  const body = JSON.parse(fetchMock.mock.calls[0][1].body) as TracePayload;
  expect(body.spans).toHaveLength(1);
  expect(body.spans[0].status).toBe('error');
  expect(body.spans[0].attributes['error.type']).toBe('rate_limit');
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm exec vitest run __tests__/integrations/openai-agents.test.ts`
Expected: FAIL — runner bez `name`/`tools` trafia w ścieżkę agenta; span ma name `agent.unknown` zamiast `agent.assistant`, brak instrumentacji toolów agenta

- [ ] **Step 3: Implementacja**

W `src/integrations/openai-agents.ts`:

(a) Nowy interfejs i detekcja na poziomie modułu:

```typescript
interface RunnerLike {
  run(agent: unknown, ...args: unknown[]): Promise<unknown>;
  [key: string | symbol]: unknown;
}

function isRunnerLike(value: AgentLike | RunnerLike): value is RunnerLike {
  return (
    typeof value.run === 'function' &&
    (value as AgentLike).name === undefined &&
    (value as AgentLike).tools === undefined &&
    (value as AgentLike).handoffs === undefined
  );
}
```

(b) Wydziel logikę instrumentacji agenta do prywatnej funkcji `instrumentAgent(agent, tracelyxClient)` (przenieś tam CAŁE dotychczasowe ciało `instrumentOpenAIAgents` — patch `run`, `wrapTools`, handoffs z Task 6, guard `INSTRUMENTED`). Publiczna funkcja staje się dispatcherem:

```typescript
export function instrumentOpenAIAgents<T extends AgentLike | RunnerLike>(
  agentOrRunner: T,
  tracelyxClient: TracelyxClient,
): T {
  if (isRunnerLike(agentOrRunner)) {
    return instrumentRunner(agentOrRunner, tracelyxClient) as T;
  }
  return instrumentAgent(agentOrRunner as AgentLike, tracelyxClient) as T;
}
```

(c) `instrumentRunner` — span wokół `runner.run`, instrumentacja przekazanego agenta:

```typescript
function instrumentRunner<T extends RunnerLike>(runner: T, tracelyxClient: TracelyxClient): T {
  const runnerAsAny = runner as any;
  if (runnerAsAny[INSTRUMENTED]) return runner;

  const originalRun = runnerAsAny.run.bind(runnerAsAny);

  runnerAsAny.run = async function (agentArg: unknown, ...args: unknown[]): Promise<unknown> {
    const agent = (agentArg ?? {}) as AgentLike;
    // instrumentuj tools/handoffs agenta, ale NIE jego .run (runner nie woła agent.run)
    if (agent !== null && typeof agent === 'object') {
      const handoffTargets = new Set<string>();
      if (Array.isArray(agent.tools)) wrapTools(agent.tools, tracelyxClient, handoffTargets);
      if (Array.isArray(agent.handoffs)) instrumentHandoffTargets(agent.handoffs, tracelyxClient);
    }

    const ctx = getActiveContext();
    const spanId = randomUUID();
    const traceId = ctx?.traceId ?? randomUUID();
    const parentSpanId = ctx?.spanId ?? null;
    const startTime = Date.now();
    const agentName = agent.name ?? 'unknown';

    const attributes: Record<string, unknown> = {
      'agent.name': agentName,
      ...(agent.model !== undefined && { 'openai.model': agent.model }),
    };

    let status: 'ok' | 'error' = 'ok';

    try {
      return await runWithContext({ spanId, traceId, tenantId: ctx?.tenantId }, () =>
        originalRun(agentArg, ...args),
      );
    } catch (error) {
      status = 'error';
      attributes['error.type'] = classifyError(error);
      if (error instanceof Error) {
        attributes['error.message'] = error.message;
        attributes['error.stack'] = error.stack;
      }
      throw error;
    } finally {
      const endTime = Date.now();
      tracelyxClient.recordSpan({
        id: spanId,
        traceId,
        parentSpanId,
        name: `agent.${agentName}`,
        kind: 'agent_step',
        startTime,
        endTime,
        durationMs: endTime - startTime,
        status,
        attributes,
        tenantId: ctx?.tenantId,
      });
    }
  };

  runnerAsAny[INSTRUMENTED] = true;
  return runner;
}
```

(d) Wydziel pętlę handoffów z Task 6 do funkcji `instrumentHandoffTargets(handoffs: unknown[], tracelyxClient: TracelyxClient): void` i użyj jej w OBU miejscach (`instrumentAgent` i `instrumentRunner`) — DRY.

Uwaga na `handoffTargets` w runnerze: tool spany `transfer_to_*` dostają `handoff.target_agent` bezpośrednio (Task 6b), a agregat na spanie runa pomijamy w wariancie runnera (set jest lokalny per call — świadome uproszczenie; agregat pozostaje w wariancie `instrumentAgent`).

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm exec vitest run __tests__/integrations/openai-agents.test.ts`
Expected: PASS (17 testów — 10 starych + 1 z Task 2 + 3 z Task 6 + 3 nowe)

- [ ] **Step 5: Run full suite + typecheck**

Run: `pnpm exec vitest run && pnpm check-types`
Expected: PASS / brak błędów

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/integrations/openai-agents.ts packages/core/__tests__/integrations/openai-agents.test.ts
git commit -m "feat(openai-agents): support Runner instrumentation (agentOrRunner)"
```

---

### Task 8: `tracelyx validate` — potwierdzenie receipt przez GET + weryfikacja routingu tenant (TASK-230)

**Files:**
- Modify: `packages/core/bin/tracelyx.ts` (`runValidateCommand`, ~linie 131–207)
- Test: `packages/core/__tests__/bin/validate.test.ts`

**Interfaces:**
- Consumes: kontrakt backendu `GET {endpoint}/v1/traces/:traceId` → 200 `{ id, tenantId?, ... }` | 404 (patrz sekcja „Kontrakt z backendem").
- Produces: pełny przepływ AC TASK-230: POST test trace → poll GET (maks. 5 prób, odstęp `TRACELYX_VALIDATE_RETRY_DELAY_MS`, default 1000 ms) → przy `--tenant` weryfikacja `tenantId` w odpowiedzi → sukces/exit 1.

- [ ] **Step 1: Write the failing tests**

W `__tests__/bin/validate.test.ts` — nowe testy + aktualizacja istniejących. Mock `fetch` musi teraz rozróżniać POST i GET. Dodaj helper na górze pliku (pod importami):

```typescript
function mockPostOkGetTrace(getResponses: Array<{ status: number; body?: unknown }>) {
  let getCall = 0;
  return vi.fn().mockImplementation((_url: string, init?: RequestInit) => {
    if (init?.method === 'POST') {
      return Promise.resolve(new Response('{"accepted":1}', { status: 200 }));
    }
    const r = getResponses[Math.min(getCall++, getResponses.length - 1)];
    return Promise.resolve(new Response(JSON.stringify(r.body ?? {}), { status: r.status }));
  });
}
```

Nowe testy (w `describe('validate command')`; każdy stubuje `TRACELYX_VALIDATE_RETRY_DELAY_MS=0`, żeby nie czekać):

```typescript
it('confirms receipt via GET after POST and exits 0', async () => {
  vi.stubEnv('TRACELYX_VALIDATE_RETRY_DELAY_MS', '0');
  const fetchMock = mockPostOkGetTrace([{ status: 200, body: { id: 't-1' } }]);
  vi.stubGlobal('fetch', fetchMock);

  const stdoutLines: string[] = [];
  vi.spyOn(process.stdout, 'write').mockImplementation((chunk) => {
    stdoutLines.push(String(chunk));
    return true;
  });
  const exitSpy = vi.spyOn(process, 'exit').mockImplementation(((code?: number) => {
    throw new Error(`exit(${code})`);
  }) as (code?: number) => never);

  try {
    await runValidateCommand(['--api-key', 'tl_test', '--project-id', 'proj_1']);
  } catch {
    // expected
  }

  expect(exitSpy).toHaveBeenCalledWith(0);
  const getCalls = fetchMock.mock.calls.filter(([, init]) => init?.method !== 'POST');
  expect(getCalls.length).toBeGreaterThanOrEqual(1);
  expect(String(getCalls[0][0])).toMatch(/\/v1\/traces\/[0-9a-f-]+$/);
  expect(stdoutLines.join('')).toContain('✓ Tracelyx configured correctly');
});

it('retries GET on 404 and succeeds when trace appears', async () => {
  vi.stubEnv('TRACELYX_VALIDATE_RETRY_DELAY_MS', '0');
  const fetchMock = mockPostOkGetTrace([{ status: 404 }, { status: 404 }, { status: 200, body: { id: 't-1' } }]);
  vi.stubGlobal('fetch', fetchMock);
  const exitSpy = vi.spyOn(process, 'exit').mockImplementation(((code?: number) => {
    throw new Error(`exit(${code})`);
  }) as (code?: number) => never);
  vi.spyOn(process.stdout, 'write').mockImplementation(() => true);

  try {
    await runValidateCommand(['--api-key', 'tl_test', '--project-id', 'proj_1']);
  } catch {
    // expected
  }
  expect(exitSpy).toHaveBeenCalledWith(0);
});

it('exits 1 when trace never becomes retrievable (receipt not confirmed)', async () => {
  vi.stubEnv('TRACELYX_VALIDATE_RETRY_DELAY_MS', '0');
  const fetchMock = mockPostOkGetTrace([{ status: 404 }]);
  vi.stubGlobal('fetch', fetchMock);
  const stdoutLines: string[] = [];
  vi.spyOn(process.stdout, 'write').mockImplementation((chunk) => {
    stdoutLines.push(String(chunk));
    return true;
  });
  const exitSpy = vi.spyOn(process, 'exit').mockImplementation(((code?: number) => {
    throw new Error(`exit(${code})`);
  }) as (code?: number) => never);

  try {
    await runValidateCommand(['--api-key', 'tl_test', '--project-id', 'proj_1']);
  } catch {
    // expected
  }
  expect(exitSpy).toHaveBeenCalledWith(1);
  expect(stdoutLines.join('')).toContain('accepted but could not be confirmed');
});

it('verifies tenant routing: exits 1 when GET returns different tenantId', async () => {
  vi.stubEnv('TRACELYX_VALIDATE_RETRY_DELAY_MS', '0');
  const fetchMock = mockPostOkGetTrace([{ status: 200, body: { id: 't-1', tenantId: 'other-corp' } }]);
  vi.stubGlobal('fetch', fetchMock);
  const stdoutLines: string[] = [];
  vi.spyOn(process.stdout, 'write').mockImplementation((chunk) => {
    stdoutLines.push(String(chunk));
    return true;
  });
  const exitSpy = vi.spyOn(process, 'exit').mockImplementation(((code?: number) => {
    throw new Error(`exit(${code})`);
  }) as (code?: number) => never);

  try {
    await runValidateCommand(['--api-key', 'tl_test', '--project-id', 'proj_1', '--tenant', 'acme-corp']);
  } catch {
    // expected
  }
  expect(exitSpy).toHaveBeenCalledWith(1);
  expect(stdoutLines.join('')).toContain('tenant routing');
});

it('verifies tenant routing: exits 0 when GET returns matching tenantId', async () => {
  vi.stubEnv('TRACELYX_VALIDATE_RETRY_DELAY_MS', '0');
  const fetchMock = mockPostOkGetTrace([{ status: 200, body: { id: 't-1', tenantId: 'acme-corp' } }]);
  vi.stubGlobal('fetch', fetchMock);
  vi.spyOn(process.stdout, 'write').mockImplementation(() => true);
  const exitSpy = vi.spyOn(process, 'exit').mockImplementation(((code?: number) => {
    throw new Error(`exit(${code})`);
  }) as (code?: number) => never);

  try {
    await runValidateCommand(['--api-key', 'tl_test', '--project-id', 'proj_1', '--tenant', 'acme-corp']);
  } catch {
    // expected
  }
  expect(exitSpy).toHaveBeenCalledWith(0);
});
```

**Aktualizacja istniejących testów:** każdy test, który mockował `fetch` przez `mockResolvedValue(new Response(..., { status: 200 }))` i oczekiwał exit 0 (`exits 0 and prints success...`, `outputs JSON...`, `sends correct payload with tenant...`, `reads api-key from env...`, `reads project-id from env...`, `success message includes app URL`), musi: (1) stubować `TRACELYX_VALIDATE_RETRY_DELAY_MS=0`, (2) użyć `mockPostOkGetTrace([{ status: 200, body: { id: 't-1', tenantId: 'acme-corp' } }])` zamiast prostego mocka (dla testu `--tenant` body musi zawierać zgodny `tenantId`). Testy 401 / network error / non-ok POST zostają bez zmian (failują przed fazą GET).

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm exec vitest run __tests__/bin/validate.test.ts`
Expected: FAIL — nowe testy czerwone (GET nie jest wykonywany)

- [ ] **Step 3: Implementacja**

W `bin/tracelyx.ts`, w `runValidateCommand`, zastąp końcowy blok `if (response.status === 401) ... else if (response.ok) ... else ...`:

```typescript
  if (response.status === 401) {
    out({ ok: false, error: 'ERROR: API key is invalid or expired. Get a new key at https://app.tracelyx.dev' });
    process.exit(1);
    return;
  }
  if (!response.ok) {
    out({ ok: false, error: `ERROR: Server returned ${response.status}.` });
    process.exit(1);
    return;
  }

  // Receipt confirmation: poll GET /v1/traces/:id until the trace is queryable.
  const retryDelayMs = parseInt(process.env['TRACELYX_VALIDATE_RETRY_DELAY_MS'] ?? '1000', 10);
  let received: Record<string, unknown> | null = null;

  for (let attempt = 0; attempt < 5; attempt++) {
    if (attempt > 0) await new Promise((resolve) => setTimeout(resolve, retryDelayMs));
    try {
      const getResponse = await fetch(`${endpoint}/v1/traces/${testTraceId}`, {
        headers: { Authorization: `Bearer ${apiKey}` },
      });
      if (getResponse.ok) {
        received = (await getResponse.json()) as Record<string, unknown>;
        break;
      }
    } catch {
      // transient network error — retry
    }
  }

  if (received === null) {
    out({
      ok: false,
      error:
        'ERROR: Test trace was accepted but could not be confirmed via GET /v1/traces/' +
        `${testTraceId}. Check the ingestion pipeline (or that the ingest API exposes GET /v1/traces/:id).`,
    });
    process.exit(1);
    return;
  }

  if (tenantId && received['tenantId'] !== tenantId) {
    out({
      ok: false,
      error: `ERROR: tenant routing failed — expected tenantId "${tenantId}", got "${String(received['tenantId'])}".`,
    });
    process.exit(1);
    return;
  }

  out({
    ok: true,
    message: `✓ Tracelyx configured correctly. Test trace ID: ${testTraceId} visible at https://app.tracelyx.dev/traces/${testTraceId}`,
  });
  process.exit(0);
```

Usuń też komentarz `// ── validate command (stub — tests added in Task 7) ──` → zamień na `// ── validate command ──`.

Budżet czasu: 5 prób × 1 s odstępu mieści się (razem z POST) w wymogu „Timeout 10 sekund" z AC.

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm exec vitest run __tests__/bin/validate.test.ts`
Expected: PASS (17 testów)

- [ ] **Step 5: Commit**

```bash
git add packages/core/bin/tracelyx.ts packages/core/__tests__/bin/validate.test.ts
git commit -m "feat(bin): confirm trace receipt via GET and verify tenant routing in validate"
```

---

### Task 9: Dokumentacja, bundle size i finalna weryfikacja

**Files:**
- Modify: `packages/core/README.md`

**Interfaces:**
- Consumes: wszystko z Tasków 1–8.
- Produces: README opisujące nowe zachowania; potwierdzony limit < 20KB gzip; zielony pełny build monorepo.

- [ ] **Step 1: Zaktualizuj README**

W `packages/core/README.md` dodaj/zaktualizuj sekcje (dopasuj do istniejącej struktury pliku — czytaj go przed edycją):

1. **Error classification** — krótki akapit: spany ze statusem `error` dostają automatycznie `attributes['error.type']` (`tool_timeout | context_window_exceeded | json_parse_error | rate_limit | network_error | hook_error | unknown`); `classifyError` jest też eksportowany publicznie.
2. **LangGraph** — dopisek: per-node spany przez `stream()` ORAZ `streamEvents()` (dokładniejsze czasy; wymaga `@langchain/langgraph >= 0.2.0`); atrybuty `langgraph.node_name`, `langgraph.thread_id`, `langgraph.checkpoint_id`; subgrafy zagnieżdżają się automatycznie.
3. **OpenAI Agents** — dopisek: `instrumentOpenAIAgents` przyjmuje agenta LUB runnera (`runner.run(agent, input)`); agenty z `handoffs` są instrumentowane rekurencyjnie — jeden trace na cały multi-agent run.
4. **CLI `validate`** — dopisek: komenda potwierdza receipt przez `GET /v1/traces/:id` i przy `--tenant` weryfikuje routing.

- [ ] **Step 2: Bundle size check**

Run (z `packages/core`): `pnpm build && gzip -c dist/index.js | wc -c`
Expected: wynik < 20480 (bajtów). Jeśli przekroczone — przejrzyj importy (np. czy `errors.ts` nie ciągnie zbędnych zależności) i zgłoś w podsumowaniu wykonania.

- [ ] **Step 3: Pełna weryfikacja monorepo**

Run (z repo root): `pnpm build && pnpm test && pnpm check-types`
Expected: wszystko zielone

- [ ] **Step 4: Commit**

```bash
git add packages/core/README.md
git commit -m "docs(core): document errorType, streamEvents, Runner support and validate receipt"
```

---

## Mapowanie tasków planu na roadmapę

| Task planu | Task roadmapy | Kryteria akceptacji |
|---|---|---|
| 1, 2 | TASK-221 (część SDK) | „SDK automatycznie wypełnia errorType na podstawie wyjątku (catch + classify)" |
| 3 | TASK-211 | `attributes['langgraph.node_name']`, `thread_id` propagowany |
| 4 | TASK-211 | patch streamowania (odpowiednik `astream_events`), kompatybilność >= 0.2.0 |
| 5 | TASK-211 | „Subgrafy: span subgraph jest child span parent graph node" |
| 6 | TASK-212 | „Trace propagowana przez handoffs — trace ID ten sam dla całego multi-agent run", `handoff.target_agent` |
| 7 | TASK-212 | `instrumentOpenAIAgents(agentOrRunner, ...)` |
| 8 | TASK-230 | „(3) potwierdza receipt przez GET na trace", „--tenant … weryfikuje routing" |
| 9 | AD-03 | bundle < 20KB gzip, dokumentacja |
